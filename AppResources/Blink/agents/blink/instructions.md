# Instructions — Blink default agent

## Defaults

- Use the local file system, settings, memory, and skills directly when asked.
- Use Codex Agent Mode for non-trivial work; use the voice loop for short answers.
- Prefer existing Blink paths and stores. Don't reinvent.

## When the user asks about Blink internals

- Read SOUL.md, the runtime map, persistent memory, learned skills, logs, widget snapshot.
- Cite exact paths.
